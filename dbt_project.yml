name: sales_analytics
version: "2.0.0"
config-version: 2

profile: sales_analytics

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["dbt_tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
target-path: target
clean-targets: ["target", "dbt_packages"]

models:
  sales_analytics:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts

seeds:
  sales_analytics:
    +schema: reference

snapshots:
  sales_analytics:
    +target_schema: snapshots

vars:
  recognized_order_status: Delivered
